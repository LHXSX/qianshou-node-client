# dependencies
node_modules/
.pnp
.pnp.js

# build output
dist/
src-tauri/target/
src-tauri/gen/

# editor
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# env / secrets
.env
.env.local

# Tauri 签名私钥 · 严禁 push 到 GitHub! (放 GH Secrets 里)
.tauri-keys-2026/*.key
.tauri-keys/*.key
**/qianshou-*.key

# CI build artifacts
src-tauri/gen/schemas/

# 系统/IDE
Thumbs.db
.DS_Store
.env.*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Tauri build artifacts
*.dmg
*.app
*.msi
*.exe
*.deb
*.AppImage

# tauri-cli pull tauri-build cache
src-tauri/Cargo.lock
.tauri-keys/

# 国内 cargo 镜像配置 (本地 · CI 用官方)
src-tauri/.cargo/

# 2026-05-28 · prebake runtime 不再 commit · CI 各平台 runner 跑 prebake-runtime.sh 各自下载
#   原因: 早期把 mac-arm64 cpython (326M) commit 进 git · 导致 mac Intel + Windows 装上跑不动
#   保留: resources/{bin,runtime}/README.md (说明用 · ! 取反)
src-tauri/resources/runtime/cpython/
src-tauri/resources/runtime/envs/
src-tauri/resources/runtime/manifest.json
src-tauri/resources/bin/uv-*
src-tauri/resources/bin/uv.exe
