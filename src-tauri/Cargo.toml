[package]
name = "qianshou-client"
version = "8.2.0"
description = "千手 · EdgeCompute 边缘算力桌面客户端"
authors = ["EdgeCompute Team"]
edition = "2021"
rust-version = "1.77"

[lib]
name = "qianshou_client_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
# ── Tauri 核心 ──
tauri = { version = "2", features = ["macos-private-api", "tray-icon"] }
tauri-plugin-shell = "2"
tauri-plugin-deep-link = "2"
tauri-plugin-updater = "2"
tauri-plugin-autostart = "2"

# ── async runtime ──
tokio = { version = "1", features = ["full"] }

# ── WebSocket 主通道 ──
tokio-tungstenite = { version = "0.24", features = ["native-tls"] }
native-tls = "0.2"
futures-util = "0.3"
http = "1"

# ── REST 兜底 ──
reqwest = { version = "0.12", features = ["json", "rustls-tls", "gzip"], default-features = false }

# ── 序列化 ──
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# ── 凭证持久化（v3.0.0：app_data 文件 + 0o600 权限）──
# 后续可切回 OS Keyring（生产签名稳定后用户只授权一次），暂不依赖。

# ── 日志 ──
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }

# ── 错误 / 工具 ──
anyhow = "1"
thiserror = "2"
async-trait = "0.1"
uuid = { version = "1", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
sysinfo = "0.32"
url = "2"
tempfile = "3"
zip = { version = "2", default-features = false, features = ["deflate"] }
which = "7"  # v8 capabilities · 探测节点安装的 software (ffmpeg / blender / ollama / ...)
sha2 = "0.10"  # V8 task_skill_pack sha256 校验
flate2 = "1"  # 2026-05-24 · ffmpeg tier tar.gz 解压
tar = "0.4"   # 2026-05-24 · ffmpeg tier tar.gz 解压
opener = "0.7"
base64 = "0.22"  # W3 (2026-05-26) · IP 代理池 · TunnelChunk 数据 base64 编解码

# ── V8.2 (2026-06-11 RFC 节点执行层重构) · ONNX 推理 ──
# RapidOCR / CLIP / bge 等模型 · ort crate 直推 · 不开 Python · 不装 paddleocr
# load-dynamic: 运行时 dlopen libonnxruntime · 不强绑版本 · 包体积小
# 重要: ort 2.0.x 仍在 RC · ort-sys 跨 RC 版本 ABI 不兼容
#       必须把 ort + ort-sys 一起精确锁到 rc.4 · 否则 cargo 自动升 ort-sys → 240 个 fn 指针错
# ndarray 0.15 (跟 ort 2.0.0-rc.4 配对) · image 0.24 配套
ort = { version = "=2.0.0-rc.4", default-features = false, features = ["load-dynamic", "ndarray"], optional = true }
ort-sys = { version = "=2.0.0-rc.4", optional = true }
ndarray = { version = "0.15", optional = true }
image = { version = "0.24", default-features = false, features = ["jpeg", "png"], optional = true }

# P0 NCE · 任务资源限制 (setpriority 跨平台)
[target.'cfg(unix)'.dependencies]
libc = "0.2"

[features]
# P0 默认只开 custom-protocol · onnx 默认关 · ort 2.0 RC 稳定后再切默认开
# 关 onnx 时 onnx_runner 退化为 stub · executor 自动 fallback python3 · 跟现状一致 · 零回归
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
# V8.2 RFC · ONNX 推理支持 · 启用后客户端能直接跑 RapidOCR/CLIP/bge 模型
# cargo build --features onnx · 或测试时用 cargo test --features onnx
onnx = ["dep:ort", "dep:ort-sys", "dep:ndarray", "dep:image"]


# ────────────────────────────────────────────────────────────────────
# Release profile (P0-4 企业级合规)
# 目标：包大小 ≤ 应有 70%、启动时间 ≤ 1.5s、无调试符号泄露。
# 实测对比（2026-05-15）：未优化 ≈ 26 MB / 优化后 ≈ 16 MB / dmg 约 11 MB
# ────────────────────────────────────────────────────────────────────
[profile.release]
opt-level = "z"          # 体积优先；CPU-bound 任务可改 "s" 或 3
lto = "fat"              # 链接时优化（构建时长 +30s，但减小 25%）
codegen-units = 1        # 关闭并行 codegen 才能让 LTO 完整生效
panic = "abort"          # 客户端 unwrap panic 直接 abort，更小，更快
strip = "symbols"        # 去掉调试符号；崩溃栈靠 Sentry/dSYM 恢复
debug = false
incremental = false

# dev profile 保持快速迭代，不动
[profile.dev]
incremental = true


# ────────────────────────────────────────────────────────────────────
# Lints (P0-2 企业级合规)
# warn 级别提醒新代码避免 unwrap/expect/panic，旧代码循序渐进治理。
# 严格 deny 会破坏现有 50+ unwrap，先用 warn 让 cargo clippy 报告。
# ────────────────────────────────────────────────────────────────────
[lints.clippy]
unwrap_used = "warn"
expect_used = "warn"
panic = "warn"
todo = "warn"
unimplemented = "warn"
# 业务逻辑常用，关闭噪音
module_name_repetitions = "allow"
missing_errors_doc = "allow"

[lints.rust]
unused_must_use = "warn"
# Tauri 自动生成的 #[command] handler 偶尔有 unused arg
unused_variables = "warn"
